import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const JointCourses = () => {
    return (
        <View style={styles.container}>
            <Text>JointCourses</Text>
        </View>
    )
}

export default JointCourses

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: '#fff',
    },
})
